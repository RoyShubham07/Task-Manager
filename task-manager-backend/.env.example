PORT=3000
# Comma-separated frontend URLs for CORS (e.g. https://your-app.vercel.app). Omit for local dev.
# FRONTEND_URL=

DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
# Set to the real password for this user (required if MySQL rejects "using password: NO"):
DB_PASSWORD=
DB_DATABASE=task_manager
# Set to true for managed MySQL that requires TLS (Railway, PlanetScale, etc.)
# DB_SSL=
